from distutils.core import setup

setup (name = 'location_tagger',
       version = '1.0',
       description = 'Use the LocAnalyzer to match a query to a region',
       py_modules=['location_tagger'])
